import CareerValuesDiagram from "@/diagram";

export default function DiagramPage() {
  return (
    <div style={{ minHeight: "100vh", display: "flex", alignItems: "center", justifyContent: "center", background: "#fff" }}>
      <CareerValuesDiagram />
    </div>
  );
}
